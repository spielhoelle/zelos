version: "3.9"
services:
    app: 
        build: .
        container_name: zelos
        command: bundle exec rails s -p 3000 -b '0.0.0.0'
        volumes: 
            - ".:/app"
        ports: 
            - "3000:3000"
        environment:
            # TODO not used in database.yml
            DB_NAME: zelos
            DB_USER: root
            DB_PASSWORD: ZRKFQdDjqJggNDk66DHd
            DB_HOST: gmysql
        networks:
          - mysqlnet

networks: 
  mysqlnet: 
    external: true